services:
  api:
    build:
      context: ./api
    environment:
      SUPABASE_DB_URL: ${SUPABASE_DB_URL:?SUPABASE_DB_URL no está definido. Copia .env.example a .env y completa la URL JDBC de Supabase.}
      DB_SCHEMA: ${DB_SCHEMA:-aulatrack}
      PORT: 8080
    ports:
      - "8080:8080"

  webapp-react:
    build:
      context: ./webapp-react
      args:
        VITE_API_URL: http://localhost:8080
    depends_on:
      - api
    ports:
      - "${REACT_PORT:-5173}:80"

  webapp-ng:
    build:
      context: ./webapp-ng
    depends_on:
      - api
    ports:
      - "${ANGULAR_PORT:-4200}:80"
